<section class="container-fluid justify-content-center">
    <div class="row justify-content-center">
        <div class="drink-image col-6 justify-content-center m-4 mt-5" id="drink-image">
            {{!-- <h2 class="mt-5">* Display Drink Image *</h2> --}}
        </div>
        <div class="col-6">
            <div class="drink-description justify-content-center m-4 mt-5">
                <div id="drink-container">
                {{!-- <h2 class="mt-5">{{ Drink Name }}</h2> --}}
                {{!-- <p>{{ Drink Description }}</p> --}}
                </div>
            </div>
            <button id="new-drink" class="new-drink-btn btn btn-outline-dark" type="submit">I don't want this drink.</button>
            <div>
                {{> radial }}
            </div>

        </div>
        <div class="drink-description col-6 justify-content-center m-4 mt-5">
            {{!-- <h2 class="mt-5">{{ Drink Name }}</h2> --}}
            {{!-- <p>{{ Drink Description }}</p> --}}
            <button id="new-drink" class="new-drink-btn btn btn-outline-dark">I don't want this stupid drink.</button>
    </div>
</section>

<script src="/javascript/index.js"></script>
